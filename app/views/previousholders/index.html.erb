<h3 style="text-align:center">Previousholders</h3>
<div class="container">
<nav class=" navbar-expand-sm", height="100px">
    <button class="collapse navbar-toggler navbar-toggler-right" type="button" data-toggle=" collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false"    aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  <ul class="navbar-nav">
    <li class="nav-item active"><%= link_to 'New Previousholder', new_previousholder_path, class: "nav-link " %></li>
<li class="nav-item active"><%= link_to 'Back to items list', items_path, class: "nav-link"%></li>
</ul>
</nav>
</div>
<br/>
<div class="container">
 <table class="table table-bordered table-hover" height="50px">
    <thead>
      <tr>
        <th scope="col">From date</th>
        <th scope="col">To date</th>
        <th scope="col">Remark</th>
        <th scope="col">Item</th>
        <th scope="col" colspan="3">Modification</th>
      </tr>
    </thead>
    <tbody style="height:50px;overflow-y:auto;  ">
      <% @previousholders.each do |previousholder| %>
        <tr>
          <td><%= previousholder.from_date %></td>
          <td><%= previousholder.to_date %></td>
          <td><%= previousholder.remark %></td>
          <td><%= previousholder.item_id %></td>
          <td><%= link_to 'Show', previousholder, class: "btn btn-info btn-sm" %></td>
          <td><%= link_to 'Edit', edit_previousholder_path(previousholder), class: "btn btn-success btn-sm" %></td>
          <td><%= link_to 'Destroy', previousholder, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger btn-sm" %></td>
        </tr>
      <% end %>
  </tbody>
</table>




